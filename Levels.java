package project;


import javafx.application.Application;
import javafx.stage.Stage;

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author hayam
 */
/* public class Levels extends Application {
    
//add it to level map class and see what happens
    @Override
    public void start(Stage primaryStage) throws Exception {
        CommonElements.levelmap.start(primaryStage);
        if(Score.totalScore==210){
            CommonElements.levelmap.startButton.setDisable(true);
            CommonElements.levelmap.level2Button.setDisable(false);
            CommonElements.levelmap.startButton.setText("Level 1 Completed!");
        }
        if(Score.totalScore==420){
            CommonElements.levelmap.startButton.setDisable(true);
            CommonElements.levelmap.level2Button.setDisable(true);
            CommonElements.levelmap.level3Button.setDisable(false);
            CommonElements.levelmap.level2Button.setText("Level 2 Completed!");
        }
        if(Score.totalScore==630){
            CommonElements.levelmap.startButton.setDisable(true);
            CommonElements.levelmap.level2Button.setDisable(true);
            CommonElements.levelmap.level3Button.setDisable(true);
            CommonElements.levelmap.level4Button.setDisable(false);
            CommonElements.levelmap.level3Button.setText("Level 3 Completed!");
        }
        if(Score.totalScore==840){
           //HERE IS SOMETHING SPECIAL????
        }
       
       
    }
} */
